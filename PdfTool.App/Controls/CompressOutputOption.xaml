<UserControl x:Class="PdfTool.App.Controls.CompressOutputOption"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             x:Name="Root">

    <!-- DataContext is the window's view model, inherited from the tab hosting this, so the preset
         is the one shown on the Compress tab rather than a copy of it. -->
    <Border Padding="10,7"
            Background="{DynamicResource SurfaceBackground}"
            BorderBrush="{DynamicResource ControlBorder}"
            BorderThickness="1"
            CornerRadius="{DynamicResource ControlCornerRadius}">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <CheckBox Grid.Column="0"
                      VerticalAlignment="Center"
                      Content="{Binding Label, ElementName=Root}"
                      IsChecked="{Binding IsChecked, ElementName=Root}" />

            <ListBox Grid.Column="1"
                     Margin="14,0,0,0"
                     Style="{StaticResource SegmentedSelector}"
                     IsEnabled="{Binding IsChecked, ElementName=Root}"
                     ItemsSource="{Binding CompressionPresets}"
                     SelectedItem="{Binding SelectedCompressionPreset}" />

            <TextBlock Grid.Column="2"
                       Style="{StaticResource MutedText}"
                       VerticalAlignment="Center"
                       Margin="14,0,0,0"
                       TextTrimming="CharacterEllipsis"
                       Text="{Binding SelectedCompressionPreset.Description}"
                       Visibility="{Binding IsChecked, ElementName=Root,
                                   Converter={StaticResource BoolToVisibility}}" />
        </Grid>
    </Border>
</UserControl>
